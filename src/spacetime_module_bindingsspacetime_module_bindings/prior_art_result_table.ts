// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.2 (commit ).

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  Identity,
  ProductType,
  ProductTypeElement,
  SubscriptionBuilderImpl,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
  type CallReducerFlags,
  type DbContext,
  type ErrorContextInterface,
  type Event,
  type EventContextInterface,
  type ReducerEventContextInterface,
  type SubscriptionEventContextInterface,
} from "@clockworklabs/spacetimedb-sdk";
import { PriorArtResult } from "./prior_art_result_type";
import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `prior_art_result`.
 *
 * Obtain a handle from the [`priorArtResult`] property on [`RemoteTables`],
 * like `ctx.db.priorArtResult`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.priorArtResult.on_insert(...)`.
 */
export class PriorArtResultTableHandle {
  tableCache: TableCache<PriorArtResult>;

  constructor(tableCache: TableCache<PriorArtResult>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<PriorArtResult> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `resultId` unique index on the table `prior_art_result`,
   * which allows point queries on the field of the same name
   * via the [`PriorArtResultResultIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.priorArtResult.resultId().find(...)`.
   *
   * Get a handle on the `resultId` unique index on the table `prior_art_result`.
   */
  resultId = {
    // Find the subscribed row whose `resultId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: bigint): PriorArtResult | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.resultId, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: PriorArtResult) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: PriorArtResult) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: PriorArtResult) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: PriorArtResult) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: PriorArtResult, newRow: PriorArtResult) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: PriorArtResult, newRow: PriorArtResult) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
